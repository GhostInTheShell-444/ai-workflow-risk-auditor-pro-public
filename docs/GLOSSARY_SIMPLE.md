# Simple Glossary

## English

- Detected = found in the text
- Calculated = computed by local rules
- Simulated = estimated scenario after protections
- Recommended = proposed action, not an obligation
- Uncertain = needs human review

## French

- Détecté = trouvé dans le texte
- Calculé = calculé par règles locales
- Simulé = scénario estimé après protections
- Recommandé = action proposée, pas obligation
- Incertain = à vérifier par un humain

## Hebrew

- זוהה = נמצא בטקסט
- מחושב = חושב לפי כללים מקומיים
- סימולציה = תרחיש מוערך אחרי הגנות
- מומלץ = פעולה מוצעת, לא חובה
- לא ודאי = דורש בדיקה אנושית

## Score Notice

English:

This score is a rule-based estimate, not a statistical probability. Score calculated from local deterministic rules. It is an aid, not a certification.

French:

Ce score est une estimation basée sur des règles locales, pas une probabilité statistique. Score calculé à partir de règles déterministes locales. C’est une aide, pas une certification.

Hebrew:

הציון הוא הערכה לפי כללים מקומיים, לא הסתברות סטטיסטית. הציון מחושב לפי כללים דטרמיניסטיים מקומיים. זו עזרה, לא הסמכה.
